import { HighlightTodoCompletedDirective } from './highlight-todo-completed.directive';

describe('HighlightTodoCompletedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTodoCompletedDirective();
    expect(directive).toBeTruthy();
  });
});
